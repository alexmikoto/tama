# tama

Tama is an IRC bot built from the ground up on Python with asyncio streams. The
purpose of this project is to create a bot with a simple plugin API inspired by
CloudBot, but overcoming some design limitations on CloudBot. Part of the scope
of this project is being mostly API compatible with the original CloudBot,
therefore several plugins are ported from CloudBot itself.
